<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - Agrovale</title>
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="sidebar">
    <h2>Agrovale</h2>
    <ul>
      <li><a href="/dashboard">🏠 Início</a></li>
      <li><a href="/produtos">📦 Produtos</a></li>
      <li><a href="/vendas">💰 Vendas</a></li>
      <li><a href="/estatisticas">📊 Estatísticas</a></li>
      <li><a href="/clientes">👤 Clientes</a></li>
      <li><a href="/relatorios/auditoria">🧾 Auditoria</a></li>
      <li><a href="/logout">🚪 Sair</a></li>
    </ul>
  </div>

  <div class="content">
    <header><h1>📦 Entrada de Mercadorias</h1></header>

    <form action="/produtos/entrada" method="POST" class="form-container">
      <div class="form-group">
        <label for="produto_id">Produto:</label>
        <select name="produto_id" id="produto_id" required>
          <option value="">Selecione um produto</option>
          <% produtos.forEach(p => { %>
            <option value="<%= p.ProdutoID %>">
              [<%= p.Codigo %>] <%= p.Nome %> — Estoque: <%= p.EstoqueAtual %> — Custo: R$ <%= (parseFloat(p.PrecoCusto) || 0).toFixed(2) %> — 
            </option>
          <% }) %>
        </select>
      </div>

      <div class="form-group">
        <label for="quantidade">Quantidade recebida:</label>
        <input type="number" name="quantidade" id="quantidade" step="0.01" min="0.01" required>
      </div>

      <div class="form-group">
        <label for="preco_custo">Novo preço de custo (R$):</label>
        <input type="number" name="preco_custo" id="preco_custo" step="0.01" min="0" required>
      </div>

      <div class="form-group">
        <label for="margem_lucro">Margem de Lucro (%):</label>
        <input type="number" step="0.01" id="margem_lucro" name="margem_lucro" value="60" required>
      </div>

      <div class="form-actions">
        <a href="/produtos" class="btn btn-secondary">Cancelar</a>
        <button type="submit" class="btn btn-success">Registrar Entrada</button>
      </div>
    </form>
  </div>
</body>
</html>
