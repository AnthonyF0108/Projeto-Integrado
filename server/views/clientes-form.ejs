<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title><%= title %> - Agrovale</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 0; background: #f5f6fa; display: flex; }

    .sidebar { width: 220px; background: #007bff; color: white; height: 100vh; position: fixed; top: 0; left: 0; padding-top: 1rem; }
    .sidebar h2 { text-align: center; margin-bottom: 2rem; }
    .sidebar a { display: block; padding: 0.8rem 1rem; color: white; text-decoration: none; font-weight: bold; }
    .sidebar a:hover { background: #0056b3; }

    .content { margin-left: 220px; padding: 2rem; width: calc(100% - 220px); }
    h1 { margin-bottom: 1rem; }

    .form-container { background: white; padding: 20px; border-radius: 8px; box-shadow: 0 2px 5px rgba(0,0,0,0.1); max-width: 600px; margin: auto; }
    .form-group { margin-bottom: 1rem; }
    label { display: block; margin-bottom: 0.5rem; font-weight: bold; }
    input { width: 100%; padding: 0.75rem; border: 1px solid #ccc; border-radius: 5px; }

    .btn { padding: 10px 16px; border: none; border-radius: 5px; cursor: pointer; font-size: 1rem; text-decoration: none; display: inline-block; }
    .btn-success { background: #28a745; color: white; }
    .btn-success:hover { background: #218838; }
    .btn-secondary { background: #6c757d; color: white; margin-left: 5px; }
    .btn-secondary:hover { background: #5a6268; }
  </style>
</head>
<body>
  <div class="sidebar">
    <h2>Agrovale</h2>
    <a href="/dashboard">🏠 Início</a>
    <a href="/produtos">📦 Produtos</a>
    <a href="/vendas">💰 Vendas</a>
    <a href="/estatisticas">📊 Estatísticas</a>
    <a href="/clientes">👤 Clientes</a>
    <a href="/logout">🚪 Sair</a>
  </div>

  <div class="content">
    <h1><%= title %></h1>
    <div class="form-container">
      <form action="<%= cliente ? '/clientes/alterar/' + cliente.ClienteID : '/clientes/novo' %>" method="POST">
        
        <div class="form-group">
          <label for="nome">Nome</label>
          <input type="text" id="nome" name="nome" value="<%= cliente ? cliente.Nome : '' %>" required>
        </div>

        <div class="form-group">
          <label for="cpf_cnpj">CPF/CNPJ</label>
          <input type="text" id="cpf_cnpj" name="cpf_cnpj" value="<%= cliente ? cliente.CPF_CNPJ : '' %>" required>
        </div>

        <div class="form-group">
          <label for="email">E-mail</label>
          <input type="email" id="email" name="email" value="<%= cliente ? cliente.Email : '' %>">
        </div>

        <div class="form-group">
          <label for="telefone">Telefone</label>
          <input type="text" id="telefone" name="telefone" value="<%= cliente ? cliente.Telefone : '' %>">
        </div>

        <div class="form-group">
          <label for="endereco">Endereço</label>
          <input type="text" id="endereco" name="endereco" value="<%= cliente ? cliente.Endereco : '' %>">
        </div>

        <button type="submit" class="btn btn-success">Salvar</button>
        <a href="/clientes" class="btn btn-secondary">Cancelar</a>
      </form>
    </div>
  </div>
</body>
</html>
