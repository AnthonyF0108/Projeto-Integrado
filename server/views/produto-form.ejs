<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <style>
    /* Estilos gerais */
    body { font-family: Arial, sans-serif; margin: 0; background: #f5f6fa; display: flex; }
    .sidebar { width: 220px; background: #007bff; color: white; height: 100vh; position: fixed; top: 0; left: 0; display: flex; flex-direction: column; padding-top: 1rem; }
    .sidebar a { padding: 0.8rem 1rem; color: white; text-decoration: none; }
    .sidebar a:hover { background: #0056b3; }
    .content { margin-left: 220px; padding: 2rem; width: calc(100% - 220px); }
    .form-container { background: white; padding: 20px; border-radius: 8px; box-shadow: 0 2px 5px rgba(0,0,0,0.1); max-width: 600px; margin: auto; }
    .form-group { margin-bottom: 1rem; }
    label { display: block; margin-bottom: 0.5rem; font-weight: bold; }
    input, select { width: 100%; padding: 0.75rem; border: 1px solid #ccc; border-radius: 5px; box-sizing: border-box; }
    .btn { padding: 0.75rem 1.5rem; border: none; border-radius: 5px; cursor: pointer; color: white; font-size: 1rem; }
    .btn-success { background: #28a745; }
    .btn-success:hover { background: #218838; }
    .btn-secondary { background: #6c757d; }
    .btn-secondary:hover { background: #5a6268; }
  </style>
</head>
<body>
  <div class="sidebar">
    <h2>Agrovale</h2>
    <a href="/dashboard">🏠 Início</a>
    <a href="/produtos">📦 Consulta de Produto</a>
    <a href="/vendas">💰 Vendas</a>
    <a href="/estatisticas">📊 Estatísticas</a>
  </div>

  <div class="content">
    <h1><%= title %></h1>
    <div class="form-container">
      <form action="/produtos/<%= produto ? 'alterar/' + produto.ProdutoID : 'criar' %>" method="POST">
        
        <div class="form-group">
          <label for="codigo_externo">Código Externo:</label>
          <input type="text" id="codigo_externo" name="codigo_externo" value="<%= produto ? produto.Codigo : '' %>" required>
        </div>
        
        <div class="form-group">
          <label for="nome_produto">Nome do Produto:</label>
          <input type="text" id="nome_produto" name="nome_produto" value="<%= produto ? produto.Nome : '' %>" required>
        </div>
        
        <div class="form-group">
          <label for="categoria">Categoria:</label>
          <input type="text" id="categoria" name="categoria" value="<%= produto ? produto.Categoria : '' %>" required>
        </div>

        <div class="form-group">
          <label for="subcategoria">Subcategoria:</label>
          <input type="text" id="subcategoria" name="subcategoria" value="<%= produto ? produto.Subcategoria : '' %>">
        </div>
        
        <div class="form-group">
          <label for="preco_custo">Preço de Custo:</label>
          <input type="number" step="0.01" id="preco_custo" name="preco_custo" value="<%= produto ? produto.PrecoCusto : '' %>" required>
        </div>

        <div class="form-group">
            <label for="margem_lucro">Margem de Lucro (%):</label>
            <input type="number" step="0.01" id="margem_lucro" name="margem_lucro" value="<%= produto ? produto.margem_lucro : '' %>" required>
        </div>
        
        <div class="form-group">
          <label for="unidade_medida">Unidade de Medida:</label>
          <input type="number" id="unidade_medida" name="unidade_medida" value="<%= produto ? produto.UnidadeMedida : '' %>" required>
        </div>

        <div class="form-group">
          <label for="quantidade_estoque">Estoque Atual:</label>
          <input type="number" id="quantidade_estoque" name="quantidade_estoque" value="<%= produto ? produto.EstoqueAtual : '' %>" required>
        </div>

        <div class="form-group">
          <label for="estoque_minimo">Estoque Mínimo:</label>
          <input type="number" id="estoque_minimo" name="estoque_minimo" value="<%= produto ? produto.EstoqueMinimo : '' %>" required>
        </div>

        <div class="form-group">
          <label for="fornecedor_id">Código do Fornecedor:</label>
          <input type="text" id="fornecedor_id" name="fornecedor_id" value="<%= produto ? produto.FornecedorID : '' %>">
        </div>

        <button type="submit" class="btn btn-success">Salvar</button>
        <a href="/produtos" class="btn btn-secondary">Cancelar</a>
      </form>
    </div>
  </div>
</body>
</html>